# Python deps for the GitHub Actions cron pipeline.
# All runtime serving lives on Cloudflare (Workers + D1 + R2);
# the production dashboard is in `web/` and does NOT use these.

# Web Scraping (BDDK monthly + weekly bulletins, bank IR PDFs, EVDS)
requests>=2.34.2
selenium>=4.48.0
beautifulsoup4>=4.15.0
lxml>=6.1.2
webdriver-manager>=4.1.2

# Google News redirect-token decoding (src/news/sources/google_news.py).
# Lazy-imported there so the minimal-deps CI test job doesn't need it.
googlenewsdecoder>=0.1.7

# Data Processing
pandas>=3.0.5      # skip 3.0.4: yanked from PyPI
numpy>=2.5.2
openpyxl>=3.1.5      # .xlsx (newer TBB digital-banking workbooks)
xlrd>=2.0.2          # legacy .xls / BIFF (older TBB digital-banking workbooks)

# Database
sqlalchemy>=2.0.52

# Audit-report PDF extraction (fitz-only; pdfplumber removed 2026-07-15)
#
# ⚠️ CAPPED BELOW 1.28 — 1.28.0 BREAKS STATEMENT PAGE LOCATION ENTIRELY.
# Same PDF, same code, _locate_pages():
#   1.27.2.3  AKBNK 2026Q1 uncon -> {'bs_assets': 7, 'bs_liab': 8, 'off_bs': 9, 'pl': 10}
#   1.28.0    AKBNK 2026Q1 uncon -> {'off_bs': 78}
#   1.28.0    AKBNK conso / GARAN / YKBNK / TEB / ISCTR -> {}  (nothing found)
# 1.28.0 ships MuPDF 1.29.0, whose text extraction changes what
# `_fitz_page_text` returns, so the anchor match in `_locate_pages` no longer
# fires. An extraction run under it finds no balance sheet, no P&L and no
# off-balance page — it does not fail loudly, it just extracts nothing.
# Found 2026-08-02: the requirement said >=1.28.0 while local dev ran 1.27.2.3,
# so CI and the developer were on different engines and only CI was broken.
# Re-test `_locate_pages` across several filings before raising this cap.
pymupdf>=1.28.2,<1.29.0

# Cloudflare R2 (S3-compatible) for audit-report PDF storage
boto3>=1.43.83

# Utilities
python-dotenv>=1.2.3
loguru>=0.7.3
